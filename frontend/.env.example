# Backend API base URL used for server-side fetches.
API_URL=http://localhost:4000/api

# Same URL exposed to the browser (client-side calls, e.g. checkout/auth).
NEXT_PUBLIC_API_URL=http://localhost:4000/api
